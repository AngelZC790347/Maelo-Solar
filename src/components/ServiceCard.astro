---
const { class: className = "" } = Astro.props;
---

<section
  class={`flex flex-col-reverse md:flex-row justify-center items-center bg-[#f5f7fa] px-6 md:px-20 py-20 gap-12 ${className}`}
>
  <article
    class="flex flex-col gap-8 w-full md:w-3/5 text-center md:text-left prose justify-between"
  >
    <slot name="title" />
    <slot name="content" />
  </article>

  <div class="w-full md:w-2/5 flex justify-center">
    <slot name="image" />
  </div>
</section>
